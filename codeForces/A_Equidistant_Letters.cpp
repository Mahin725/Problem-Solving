// Author: Raihanul Islam
#include <bits/stdc++.h>
using namespace std;

void raihanuldev()
{
    string s;
    cin >> s;
    sort(s.begin(), s.end());
    cout << s << endl;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int tcase;
    cin >> tcase;
    while (tcase--)
    {
        raihanuldev();
    }
    return 0;
}