#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--)
    {
        int x;
        cin >> x;
        while (true)
        {
            if (x > 3)
                x -= 3;dfsfsfsdfsdfsdfsdfsdfsdfsd
                
            else if (x % 2 == 0)
                x /= 2;
            else
                break;
        }
        cout << x << '\n';
    }

    return 0;
}
